<ContentDialog
    x:Class="Milet.App.Views.Lager.TeillieferungDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Milet.App.Views.Lager"
    Title="Lieferschein erzeugen"
    PrimaryButtonText="Lieferschein erzeugen"
    CloseButtonText="Abbrechen"
    DefaultButton="Primary">
    <StackPanel Spacing="12" MinWidth="500">
        <ComboBox Header="Lagerort *" HorizontalAlignment="Stretch"
                  ItemsSource="{x:Bind Lagerorte, Mode=OneWay}"
                  SelectedValue="{x:Bind AusgewaehlterLagerortId, Mode=TwoWay}"
                  SelectedValuePath="Id" DisplayMemberPath="Bezeichnung" />
        <TextBlock Text="Zu liefernde Mengen (0 = Position wird nicht mitgeliefert):" />
        <ListView MaxHeight="360" ItemsSource="{x:Bind Zeilen}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:TeillieferungZeile">
                    <StackPanel Padding="4" Spacing="4">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="100" />
                                <ColumnDefinition Width="120" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind Bezeichnung}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{x:Bind OffeneMenge}" VerticalAlignment="Center" />
                            <NumberBox Grid.Column="2" Minimum="0" Maximum="{x:Bind OffeneMenge, Converter={StaticResource DecimalToDoubleConverter}}" SpinButtonPlacementMode="Compact"
                                       Value="{x:Bind GewaehlteMenge, Mode=TwoWay, Converter={StaticResource DecimalToDoubleConverter}}" />
                        </Grid>
                        <StackPanel Orientation="Horizontal" Spacing="8" Visibility="{x:Bind IstKulturartikel, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ComboBox Width="260" PlaceholderText="Sektion/Kulturstufe wählen..." ItemsSource="{x:Bind FundstellenOptionen}" SelectedItem="{x:Bind AusgewaehlteFundstelle, Mode=TwoWay}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SektionBezeichnung}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <TextBlock VerticalAlignment="Center" Opacity="0.7" FontSize="12" Text="{x:Bind AusgewaehlteFundstelle.StufeBezeichnung, Mode=OneWay}" />
                            <TextBlock VerticalAlignment="Center" Opacity="0.7" FontSize="12" Text="{x:Bind AusgewaehlteFundstelle.Menge, Mode=OneWay}" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</ContentDialog>
